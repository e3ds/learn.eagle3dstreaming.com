# The editor password. It gates writing to the live docs site, and it has no
# business in a repository that will get a remote later.
editor-password.txt

# Runtime log, appended to on every start by learn_launcher.bat.
learn.log

# Backups taken before an edit, not history.
*.bak

node_modules/
